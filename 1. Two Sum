class Solution(object):
    def twoSum(self, nums, target):

        numsLength = len(nums)

        for i, num1 in enumerate(nums):
            for num2 in nums[i+1:]:
        
                if target is num1 + num2:
                    return [num1, num2]
