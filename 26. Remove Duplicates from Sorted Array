class Solution(object):
    def removeDuplicates(self, nums):
        i = 0
        k = len(set(nums))
        while i < k:
            
            
            
            
            
            i += 1
