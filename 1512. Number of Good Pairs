class Solution(object):
    def numIdenticalPairs(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        count = 0

        for firstNum in nums:

            for secondNum in nums[firstNum:]:

                if firstNum == secondNum and index(firstNum) <= index(secondNum):

                    count += 1

        return count

