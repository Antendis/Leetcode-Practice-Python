class Solution(object):
    def numJewelsInStones(self, jewels, stones):
        
        output = 0
        for jewel in jewels:
            for stone in stones:
                if stone == jewel:
                    output += 1

        return output
